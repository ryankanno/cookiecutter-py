name: "{{cookiecutter.package_name}}"
windows:
- name: "project folder"
  root: "{{cookiecutter.project_root}}"
  layout: tiled
  panes:
    - commands:
      - "ls -al"
- name: "konch shell"
  root: "{{cookiecutter.project_root}}"
  layout: tiled
  panes:
    - commands:
      - "konch"
