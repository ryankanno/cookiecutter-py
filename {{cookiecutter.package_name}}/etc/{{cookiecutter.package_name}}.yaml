session_name: "{{cookiecutter.package_name}}"
windows:
- name: "project folder"
  layout: even-horizontal
  panes:
    - commands:
      - "ls -al"
- name: "konch shell"
  layout: even-horizontal
  panes:
    - commands:
      - "konch"
